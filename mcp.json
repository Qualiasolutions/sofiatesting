{
  "servers": {
    "postman": {
      "type": "stdio",
      "command": "npx",
      "args": ["@postman/postman-mcp-server@latest"],
      "env": {
        "POSTMAN_API_KEY": "REPLACE_WITH_POSTMAN_API_KEY"
      }
    }
  }
}
